version: '3.9'

services:

  gaia-catalog:
    container_name: gaia-catalog
    platform: ${DOCKER_ARCH}
    restart: unless-stopped
    links:
      - "gaia-solr:index"
    #entrypoint: ["sleep","infinity"]  
    entrypoint: ["nohup","python","ohdsi.py","&"]
